{
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["ESNext"],
    "module": "Preserve",
    "moduleResolution": "bundler",
    "types": ["bun"],

    "noEmit": true,
    "allowJs": false,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    "strict": true,

    // 打开这两个开关时实测 250 处，全部清完了（0 处），所以正式开在这里。
    //
    // 清的过程本身就是这轮最大的收获来源 —— 「声明了没读」几乎每一簇背后
    // 都是一个**接了一半的功能**：
    //   · fallbackQuestion 解构九个字段一个没用，其中一条还让纯函数能抛异常
    //   · 同伴控制命令全是门面，setControl 只有测试在调用
    //   · 整套决心系统三个方法各自只出现一次 —— 都是自己的定义
    //   · 五个空心测试：算出要检查的东西然后不看它
    //
    // 有意不用的**参数**以 `_` 开头（TS 只对参数认这个前缀，
    // 局部变量加了照报不误，那类必须真删）。
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  // ⚠ 原先这里还有 `tools/**/*.ts`，而 `tools/` 在 .gitignore 里 ——
  //   **typecheck 的结果因此取决于仓库外的文件**：本机能过、干净检出未必，
  //   反过来干净检出能过、本机未必。清死导出时就撞上了：
  //   `buildWmContext` / `neutralSupport` 全仓无人 import，只有 tools/ 里
  //   两个历史草稿在用，于是「判据说是死的、编译器说还活着」。
  //   这和摄取入口曾经躺在 tools/ 里是同一类问题：仓库状态与本机状态分叉。
  //   该入库的脚本已经搬进 scripts/，tools/ 只剩一次性草稿，不再纳入编译。
  "include": ["src/**/*.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
